# HoopoeWorld
maybe at the end I will add this to the HSL </br>
my project is both this and the satellite.
to create certificate: openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes